post:
  operationId: B06
  description: Create a billing account in both Billing System and Customer DB
  summary: Create a Billing Account in Both Billing System and Customer DB.
  tags: []
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: ../../models/billing-account/new-billing-account/NewBillingAccount.yaml
  responses:
    '200':
      description: Operation success
      content:
        application/json:
          schema:
            $ref: ../../models/common/OperationSuccess.yaml
    '401':
      description: Missing require parameter name {Parameter Name}.
      content:
        application/json:
          schema:
            $ref: ../../models/common/OperationFailed.yaml
    '405':
      description: JSON format error.
      content:
        application/json:
          schema:
            $ref: ../../models/common/OperationFailed.yaml
    '500':
      description: Request time out or internal error.
      content:
        application/json:
          schema:
            $ref: ../../models/common/OperationFailed.yaml
    '501':
      description: External error.
      content:
        application/json:
          schema:
            $ref: ../../models/common/OperationFailed.yaml