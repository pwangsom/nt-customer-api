post:
  operationId: B34
  description: Add new contact person of a billing account by bill_acct_number
  summary: Add New Contact Person of a Billing Account by BA Number.
  tags: []
  parameters:
    - $ref: ../../parameters/BillAcctNumber.yaml
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: ../../models/billing-account/update-billing-account/AddBillingContactPerson.yaml
  responses:
    '200':
      description: Operation success
      content:
        application/json:
          schema:
            $ref: ../../models/common/OperationSuccess.yaml
    '400':
      description: Bad request.
      content:
        application/json:
          schema:
            $ref: ../../models/common/OperationFailed.yaml
    '401':
      description: Authorization information is missing or invalid.
    '404':
      description: Operation failed.
      content:
        application/json:
          schema:
            $ref: ../../models/common/OperationFailed.yaml      
    '500':
      description: Unexpected error.