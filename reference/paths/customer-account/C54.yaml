post:
  operationId: C54
  description: Create an individual partner
  summary: Create An Individual Partner.
  tags: []
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: ../../models/partner/NewIndividualPartner.yaml
  responses:
    '200':
      description: Operation success
      content:
        application/json:
          schema:
            $ref: ../../models/common/OperationSuccess.yaml
    '400':
      description: Bad request.
      content:
        application/json:
          schema:
            $ref: ../../models/common/OperationFailed.yaml
    '401':
      description: Authorization information is missing or invalid.
    '404':
      description: Operation failed.
      content:
        application/json:
          schema:
            $ref: ../../models/common/OperationFailed.yaml      
    '500':
      description: Unexpected error.  