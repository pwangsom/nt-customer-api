get:
  operationId: I05
  description: Retrieve prepaid subscriptions by subscriber_number, subscription_date_from, and subscription_date_to
  summary: Get Prepaid Subscriptions by Subscriber Number.
  tags: []
  parameters:
    - $ref: ../../parameters/SubscriberNumber.yaml  
    - $ref: ../../parameters/DateFrom.yaml    
    - $ref: ../../parameters/DateTo.yaml    
  responses:
    '200':
      description: Data Found
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: ../../models/invoice-payment/PrepaidSubscriptionCharge.yaml  
    '400':
      description: Bad request.
    '401':
      description: Authorization information is missing or invalid.
    '404':
      description: Data was not found.
    '500':
      description: Unexpected error.