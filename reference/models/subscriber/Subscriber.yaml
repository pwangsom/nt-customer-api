title: Subscriber
allOf:
  - $ref: BasicSubscriber.yaml
  - type: object
    properties:
      package_list:
        type: array
        items:
          $ref: Package.yaml
      component_list:
        type: array
        items:
          $ref: Component.yaml
      subscriber_property_list:
        type: array
        items:
          $ref: SubscriberProperty.yaml
      installation_address_list:
        type: array
        items:
          $ref: InstallationAddress.yaml
x-tags:
  - Subscriber
