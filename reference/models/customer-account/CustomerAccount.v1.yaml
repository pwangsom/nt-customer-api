title: CustomerAccount
allOf:
  - $ref: ../common/CustomerAcctNumber.v1.yaml
  - type: object
    properties:
      customer_full_name:
        type: string
        example: นายสมชาย เจริญยิ่ง
        description: Customer full name.
      id_card_info:
        $ref: IdCardInfo.v1.yaml
      generic_info:
        $ref: CustomerGenericInfo.v1.yaml
      customer_acct_type:
        oneOf:
          - $ref: Individual.v1.yaml
          - $ref: Organization.v1.yaml
x-tags:
  - CustomerAccount
description: Customer full info model
