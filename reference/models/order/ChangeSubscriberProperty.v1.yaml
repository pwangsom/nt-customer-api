title: ChangeSubscriberProperty
allOf:
  - $ref: SubscriberInfo.v1.yaml
  - type: object
    properties:
      order_type:
        type: integer
        enum:
          - 5
        description: Change order
      order_subtype:
        type: integer
        enum:
          - 6
        description: Change number order
      order_no:
        type: string
        example: '25412'
        description: Reference order number
      subscriber_property:
        type: string
        example: '1234567890123456789'
        description: Subscriber property e.g. ICCID
      client_info:
        $ref: ../common/RequestClientInfo.v1.yaml
    required:
      - order_type
      - order_subtype
      - order_no
      - subscriber_property
      - client_info
x-tags:
  - ChangeSubscriberProperty
