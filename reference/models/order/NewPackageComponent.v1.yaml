title: NewPackageComponent
allOf:
  - $ref: SubscriberInfo.v1.yaml
  - type: object
    properties:
      order_type:
        type: integer
        enum:
          - 5
        description: Change order
      order_subtype:
        type: integer
        enum:
          - 2
        description: Change package
      order_no:
        type: string
        example: '25412'
        description: Reference order number
      client_info:
        $ref: ../common/RequestClientInfo.v1.yaml
    required:
      - order_type
      - order_subtype
      - order_no
      - client_info
x-tags:
  - NewPackageComponent
