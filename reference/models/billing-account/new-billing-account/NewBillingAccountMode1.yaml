title: NewBillingAccountMode1
type: object
properties:
  client_info:
    $ref: ../../common/RequestClientInfo.yaml
  reference_info:
    $ref: NewBillingReferenceInfoMode1.yaml
  general_info:
    $ref: NewBillingGeneralInfo.yaml
  dispatch_method_info:
    $ref: NewBillingDispatchMethodInfo.yaml
  billing_address_list:
    $ref: NewBillingAddress.yaml
  more_info:
    $ref: NewBillingMoreInfo.yaml
  egp_info:
    $ref: NewBillingEgpInfo.yaml
required:
  - customer_acct_number
  - bill_acct_number
  - esb_bill_acct_id
  - general_info
  - dispatch_method_info
  - billing_address_list
